package com.example.eduspace.opportunity.enums;

/** Only meaningful when mode != ONLINE. */
public enum TuitionLocationType {
    HOME_TUITION,   // tutor travels to the student
    CENTER_BASED    // student comes to the teacher's home/center
}